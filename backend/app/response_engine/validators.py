from app.response_engine.models import ResponseAction


class ResponseValidator:

    def validate(
        self,
        action: ResponseAction,
    ) -> bool:

        # later

        # user permissions

        # whitelist

        # dry-run

        # confirmation

        return True